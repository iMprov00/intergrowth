<!DOCTYPE html>
<html>
<head>
  <title>Результаты - Intergrowth-21</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
  <style>
    .percentile-value {
      font-size: 1.2rem;
      font-weight: bold;
    }
    .chart-container {
      height: 500px;
      margin: 20px 0;
    }
  </style>
</head>
<body>
  <div class="container mt-4">
    <div class="card shadow-sm mb-4">
      <div class="card-header bg-primary text-white">
        <h1 class="h4 mb-0">Результаты оценки роста плода</h1>
      </div>
      <div class="card-body">
        <div class="row">
          <div class="col-md-6">
            <h5 class="card-title">Параметры измерения</h5>
            <ul class="list-group list-group-flush mb-3">
              <li class="list-group-item d-flex justify-content-between">
                <span>Гестационный возраст:</span>
                <strong><%= @measurement.gestational_age %> недель</strong>
              </li>
              <li class="list-group-item d-flex justify-content-between">
                <span>Тип измерения:</span>
                <strong>
                  <% if @measurement.measurement_type == 'HC' %>
                    Окружность головы (HC)
                  <% elsif @measurement.measurement_type == 'BPD' %>
                    Бипариетальный размер (BPD)
                  <% elsif @measurement.measurement_type == 'AC' %>
                    Окружность живота (AC)
                  <% elsif @measurement.measurement_type == 'FL' %>
                    Длина бедренной кости (FL)
                  <% end %>
                </strong>
              </li>
              <li class="list-group-item d-flex justify-content-between">
                <span>Значение:</span>
                <strong><%= @measurement.value %> мм</strong>
              </li>
            </ul>
          </div>
          <div class="col-md-6">
            <div class="card bg-light">
              <div class="card-body text-center">
                <h5 class="card-title">Процентиль</h5>
                <div class="percentile-value text-primary">
                  <%= @measurement.percentile %>%
                </div>
                <p class="mt-2 mb-0 text-muted">
                  <% if @measurement.percentile > 90 %>
                    Выше 90-го процентиля
                  <% elsif @measurement.percentile < 10 %>
                    Ниже 10-го процентиля
                  <% else %>
                    Соответствует норме
                  <% end %>
                </p>
              </div>
            </div>
          </div>
        </div>
        
        <div class="chart-container">
          <canvas id="growthChart"></canvas>
        </div>
        
        <div class="d-grid gap-2 d-md-flex justify-content-md-end">
          <a href="/" class="btn btn-primary px-4">
            <i class="bi bi-arrow-left-circle"></i> Новый расчет
          </a>
        </div>
      </div>
    </div>
  </div>

  <script>
    const ctx = document.getElementById('growthChart').getContext('2d');
    const chartData = {
      labels: <%= @chart_data[:labels].to_json %>,
      datasets: <%= @chart_data[:datasets].to_json %>
    };
    
    new Chart(ctx, {
      type: 'line',
      data: chartData,
      options: {
        responsive: true,
        maintainAspectRatio: false,
        scales: {
          y: {
            title: { 
              display: true, 
              text: 'Размер (мм)',
              font: { weight: 'bold' }
            }
          },
          x: {
            title: { 
              display: true, 
              text: 'Гестационный возраст (недели)',
              font: { weight: 'bold' }
            }
          }
        },
        plugins: {
          legend: {
            position: 'top',
            labels: {
              font: { size: 12 }
            }
          },
          tooltip: {
            callbacks: {
              label: function(context) {
                return context.dataset.label + ': ' + context.parsed.y + ' мм';
              }
            }
          }
        }
      }
    });
  </script>

  <!-- Иконки Bootstrap -->
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.8.0/font/bootstrap-icons.css">
</body>
</html>